version: "3.8"
# Auto-generated for dev environment
services:
  postgres:
    image: postgres:15
    environment:
      POSTGRES_PASSWORD: secret
    volumes:
      - postgres_data:/var/lib/postgresql/data
    networks:
      - app_network

  redis:
    image: redis:7-alpine
    networks:
      - app_network

networks:
  app_network:
    driver: bridge

volumes:
  postgres_data:
